# Challenge Edition version - SINGLE SOURCE OF TRUTH.
# Read at runtime by the About window and the auto-updater
# (UI/Utilities/ChallengeEditionInfo.cs), and by build.bat to name the release ZIP.
# Bumping the version only means editing this file and rebuilding - no C# changes.
#
# Rules:
#   - "version" must be plain numeric (major.minor, e.g. 1.6) so the updater can compare it.
#   - Bump this for every release and keep it in sync with the version string you upload
#     alongside the ZIP in the mod panel.
version=3.3
date=2026-08-17
